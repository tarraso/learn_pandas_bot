# Руководство по датасетам для заданий

## 📊 Обзор

Таблица `Dataset` предназначена для хранения тестовых данных, которые используются для проверки кодовых заданий в боте.

## 🗄️ Структура модели Dataset

```python
class Dataset(models.Model):
    name = CharField              # Название датасета
    description = TextField       # Описание содержимого
    data = JSONField             # Данные в JSON формате
    data_format = CharField      # Формат: dict, json, csv
    is_active = BooleanField     # Активен ли датасет
    created_at = DateTimeField   # Дата создания
    updated_at = DateTimeField   # Дата обновления
```

## 📝 Форматы данных

### 1. Dictionary (dict) - Рекомендуется

**Пример:**
```json
{
    "product": ["Яблоки", "Бананы", "Апельсины"],
    "quantity": [10, 15, 8],
    "price": [50, 30, 60]
}
```

**Использование в коде:**
```python
import pandas as pd

# Создание DataFrame из датасета
df = pd.DataFrame(dataset_data)
```

**Преимущества:**
- ✅ Самый простой формат для pandas
- ✅ Легко читать и редактировать
- ✅ Прямое преобразование в DataFrame

### 2. List of Dictionaries

**Пример:**
```json
[
    {"name": "Ноутбук", "price": 50000, "category": "Электроника"},
    {"name": "Мышь", "price": 1000, "category": "Электроника"},
    {"name": "Стол", "price": 15000, "category": "Мебель"}
]
```

**Использование:**
```python
df = pd.DataFrame(dataset_data)
```

### 3. CSV String (csv)

**Пример:**
```
"product,quantity,price\nЯблоки,10,50\nБананы,15,30"
```

**Использование:**
```python
from io import StringIO

df = pd.read_csv(StringIO(dataset_data))
```

## 📚 Примеры датасетов

### 1. Простой датасет - Продажи
```python
{
    "product": ["Яблоки", "Бананы", "Апельсины", "Груши"],
    "quantity": [10, 15, 8, 12],
    "price": [50, 30, 60, 45]
}
```
**Назначение:** Базовые операции с DataFrame

### 2. Датасет с дубликатами - Заказы
```python
{
    "order_id": [1, 2, 2, 3, 4, 4],
    "customer": ["Иван", "Мария", "Мария", "Петр", "Анна", "Анна"],
    "amount": [1000, 1500, 1500, 2000, 1200, 1200]
}
```
**Назначение:** Проверка работы с `drop_duplicates()`, `duplicated()`

### 3. Датасет с пропусками - Температура
```python
{
    "date": ["2024-01-01", "2024-01-02", "2024-01-03", "2024-01-04", "2024-01-05"],
    "temperature": [20, None, 22, None, 24],
    "humidity": [65, 70, None, 75, 72]
}
```
**Назначение:** Проверка работы с `dropna()`, `fillna()`, `isna()`

### 4. Датасет - Студенты и оценки
```python
{
    "student": ["Алексей", "Мария", "Иван", "Ольга", "Петр"],
    "math": [85, 92, 78, 95, 88],
    "physics": [90, 88, 82, 91, 85],
    "chemistry": [87, 94, 80, 89, 92]
}
```
**Назначение:** Агрегация, вычисления, анализ

### 5. Датасет - Сотрудники компании
```python
{
    "name": ["Иванов И.", "Петрова М.", "Сидоров П.", "Козлова А.", "Морозов Д."],
    "department": ["IT", "HR", "IT", "Sales", "IT"],
    "salary": [80000, 60000, 90000, 70000, 85000],
    "experience": [5, 3, 8, 4, 6]
}
```
**Назначение:** GroupBy, фильтрация, сортировка

## 🎯 Использование в заданиях

### Способ 1: В admin-панели

1. Перейти в админку: https://pandadev.taras.rocks/admin/
2. Открыть раздел "Datasets"
3. Выбрать датасет или создать новый
4. Скопировать данные из поля `data`

### Способ 2: Программно через API

```python
from questions.models import Dataset

# Получить датасет по имени
dataset = Dataset.objects.get(name="Простой датасет - Продажи")

# Получить данные
data = dataset.data

# Создать DataFrame
import pandas as pd
df = pd.DataFrame(data)
```

### Способ 3: В тестовых случаях

```python
# В поле test_cases вопроса
{
    "dataset": "Простой датасет - Продажи",
    "expected_output": "...",
    "description": "Проверка фильтрации"
}
```

## 🔧 Методы модели Dataset

### `get_data_as_code()`

Возвращает данные в виде строки кода Python:

```python
dataset = Dataset.objects.get(name="Простой датасет - Продажи")
code = dataset.get_data_as_code()

# Результат:
# "{'product': ['Яблоки', 'Бананы', ...], 'quantity': [10, 15, ...]}"
```

## 📊 Доступ через админ-панель

**URL:** https://pandadev.taras.rocks/admin/questions/dataset/

**Функции:**
- ✅ Просмотр всех датасетов
- ✅ Создание новых датасетов
- ✅ Редактирование существующих
- ✅ Фильтрация по формату и статусу
- ✅ Поиск по названию и описанию
- ✅ Активация/деактивация датасетов

**Колонки списка:**
- Название
- Формат данных
- Активен
- Дата создания

## 💡 Рекомендации

### При создании датасетов:

1. **Используйте осмысленные названия**
   - ✅ "Датасет с дубликатами - Заказы"
   - ❌ "Датасет 1"

2. **Добавляйте подробные описания**
   - Укажите назначение датасета
   - Опишите какие операции можно проверять

3. **Предпочитайте формат dict**
   - Самый простой для работы
   - Легко редактировать в админке

4. **Делайте датасеты небольшими**
   - 5-10 строк обычно достаточно
   - Фокус на конкретных кейсах

5. **Используйте русские названия**
   - Для лучшего понимания учениками
   - Реалистичные примеры

### При использовании в заданиях:

1. **Выбирайте подходящий датасет**
   - Для дубликатов → "Датасет с дубликатами"
   - Для NaN → "Датасет с пропусками"

2. **Указывайте в задании**
   - Какой датасет использовать
   - Что нужно с ним сделать

3. **Проверяйте результаты**
   - Убедитесь, что ожидаемый результат правильный
   - Протестируйте на реальных данных

## 🚀 Примеры использования

### Задание: Найти дубликаты

**Датасет:** "Датасет с дубликатами - Заказы"

**Задание:**
```python
# Найдите все дублирующиеся строки в DataFrame
import pandas as pd

df = pd.DataFrame({
    "order_id": [1, 2, 2, 3, 4, 4],
    "customer": ["Иван", "Мария", "Мария", "Петр", "Анна", "Анна"],
    "amount": [1000, 1500, 1500, 2000, 1200, 1200]
})

# Ваш код здесь
duplicates = ???
```

**Правильный ответ:** `df.duplicated()`

### Задание: Заполнить пропуски

**Датасет:** "Датасет с пропусками - Температура"

**Задание:**
```python
# Заполните все пропущенные значения нулями
import pandas as pd

df = pd.DataFrame({
    "date": ["2024-01-01", "2024-01-02", "2024-01-03"],
    "temperature": [20, None, 22],
    "humidity": [65, 70, None]
})

# Ваш код здесь
df_filled = ???
```

**Правильный ответ:** `df.fillna(0)`

## 📈 Статистика

**Всего датасетов в системе:** 10

**По форматам:**
- Dictionary: 9
- CSV: 1
- JSON: 0

**По категориям:**
- Очистка данных: 3
- Общие операции: 4
- Анализ: 3

## 🎓 Лучшие практики

1. **Для новых топиков** - создавайте специализированные датасеты
2. **Для сложных заданий** - используйте несколько датасетов
3. **Для тестирования** - убедитесь что датасет активен
4. **Для реалистичности** - используйте настоящие примеры данных

## 🔗 См. также

- [Модель Question](questions/models.py:27) - Вопросы с кодовыми заданиями
- [Admin панель](questions/admin.py:46) - Управление датасетами
- [Примеры датасетов](add_sample_datasets.py) - Скрипт для создания
